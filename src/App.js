import "./App.css"

const App = ({ name }) => {
    return `
        <h2>Welcome ${name}</h2>
        <p>Build apps with plain JS but with modern tooling and modular code !!</p>
    `
}

export default App